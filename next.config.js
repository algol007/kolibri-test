/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["tailwindui.com", "kolibriforimpact.com", "images.unsplash.com"]
}
}

module.exports = nextConfig
